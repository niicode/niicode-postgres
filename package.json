{
  "name": "expressapp",
  "version": "1.0.0",
  "description": "this is to test the postgresql",
  "main": "app",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "niicode",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^16.0.2",
    "express": "^4.18.1"
  },
  "dependencies": {
    "pg": "^8.8.0"
  }
}
